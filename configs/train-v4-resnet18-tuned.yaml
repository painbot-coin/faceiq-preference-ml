# Preference comparator v4 — tuned ResNet-18 (fix v1 overfit pattern).
# v1 peaked at epoch 7 (76.6%) then val acc dropped; v2 showed bigger != better.
# Changes vs v1 ONLY: epochs 10->7, weight_decay 1e-4->3e-4.
# Run: python scripts/train.py --config configs/train-v4-resnet18-tuned.yaml

export_dir: data/exports/cmr1mr0m7000196d57zi3vcgn
run_name: train-v4-resnet18-tuned

backbone: resnet18
image_size: 224
batch_size: 64
epochs: 7
lr: 0.0001
weight_decay: 0.0003

val_fraction: 0.2
split_seed: 42
num_workers: 4
skip_ties: true
augment: true
